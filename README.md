# go-client-server

```sh
# Build everything
make

# Start server listening to port 1234 by default, or override with -p XXXX
./bin/server

# Run CLI client fetching products list
./bin/client -a localhost:1234
```
